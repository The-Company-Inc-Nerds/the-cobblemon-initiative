# ☎ The Founder — pre-finale call, the shadow self. Fires once the Board is cleared (board_cleared),
# the last beat before the mirror battle. NUMBER WITHHELD call screen (phone_calls/founder.json,
# avatar "unknown"); one-shot via done_tag call_founder_done on completion. Run AS @s.
# NOTE: dormant until act-3 Board content emits a `board_cleared` player tag (not built yet).
cobblemon-initiative phone ring founder
