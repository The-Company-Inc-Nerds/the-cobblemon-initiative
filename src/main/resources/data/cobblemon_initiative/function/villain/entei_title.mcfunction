# Scheduled reunion beat (+5s from entei_return). Server context — schedule loses the
# player, so re-target via @a (single-player contract; the memory/frag_title precedent).
execute as @a[tag=ci_entei_returned] at @s run function cobblemon_initiative:villain/entei_show
