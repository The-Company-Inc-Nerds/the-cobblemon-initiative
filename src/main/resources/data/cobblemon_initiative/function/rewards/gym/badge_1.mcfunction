# Gym 1 — Takehara Falls (Bug). Triumph layer; the frag_1 title follows +4s.
# Called as the player via the leader's command reward (same pattern as memory/gym/frag_1).
function cobblemon_initiative:rewards/badge_ceremony {name:"Falls Badge",cap:22}
