# Memory fragment reveal — the scheduled (+4s) second stage of the badge ceremony.
# Server context (schedule loses the player); single-player: @a == the player.
execute as @a at @s run function cobblemon_initiative:memory/frag_show with storage cobblemon_initiative:memory frag
