# Lucian's deliver button — take the Dead Letter prop (dialog Cmds cannot carry the
# double-quoted component predicate, so it lives here; pattern proven in surrender_letter).
clear @s minecraft:paper[minecraft:custom_name='{"color":"gold","italic":false,"text":"Dead Letter"}'] 1
