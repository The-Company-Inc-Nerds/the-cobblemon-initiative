# ☎ Mayor Liang — nudge back to him for the free FIRST beacon (freed a field, no beacon yet).
# Fired from homestead/need_beacon when Liang is freed (defeated_sq_mayor_suits) and the first
# beacon has not been given. Call screen (phone_calls/first_beacon.json); one-shot via done_tag
# call_first_beacon_done on completion (need_beacon's reminder branch keys on it). Run AS @s.
cobblemon-initiative phone ring first_beacon
