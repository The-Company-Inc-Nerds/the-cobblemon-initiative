# ☎ Mom — the strange Pokemon by home (Latios home-base reward). Call screen
# (phone_calls/mom_latios.json). Fired ONCE from HomeBaseManager the moment the block-placed
# threshold is passed (the manager guards the one-shot with the home_base_built tag, so the
# script carries NO done_tag). Run AS @s.
cobblemon-initiative phone ring mom_latios
