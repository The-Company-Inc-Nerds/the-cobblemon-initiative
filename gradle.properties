org.gradle.jvmargs=-Xmx4G
# kotlin("jvm") is applied purely for Loom's Kotlin-metadata remapping of mod deps
# (Cobblemon). Never auto-add kotlin-stdlib — this mod is pure Java.
kotlin.stdlib.default.dependency=false
