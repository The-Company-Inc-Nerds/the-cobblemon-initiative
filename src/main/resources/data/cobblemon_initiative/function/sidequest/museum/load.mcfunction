# Museum sidequest — scratch objective for count-checked turn-ins. Needs the
# #minecraft:load tag (listed in the agent report; do NOT edit function tags directly).
scoreboard objectives add ci_sq_scratch dummy
