# ☎ Mom — friendship-care intro after the 3rd badge. Rings the mod's call screen (phone_calls/mom.json).
# One-shot via the script's done_tag call_mom_watch_done, granted on COMPLETION — see phone/tick for
# the re-ring model. Run AS @s (the player is the command source).
cobblemon-initiative phone ring mom
